# API Configuration
# For local development, use your local backend URL
# For production, use your deployed backend URL (e.g., https://your-backend.railway.app/api/v1)
VITE_API_URL=http://localhost:5000/api/v1

# App Configuration
VITE_APP_NAME=Part-Time Pay$
VITE_APP_VERSION=1.0.0

# Environment
VITE_NODE_ENV=development

# Google OAuth (optional - only if using Google login)
# VITE_GOOGLE_CLIENT_ID=your-google-client-id

# Razorpay (optional - only if using payment features)
# VITE_RAZORPAY_KEY_ID=your-razorpay-key-id

# Cloudinary (optional - only if using cloud file storage)
# VITE_CLOUDINARY_CLOUD_NAME=your-cloudinary-cloud-name
